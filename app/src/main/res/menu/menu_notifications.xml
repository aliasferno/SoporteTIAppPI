<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <item
        android:id="@+id/action_refresh"
        android:icon="@android:drawable/ic_popup_sync"
        android:title="Refrescar"
        app:showAsAction="ifRoom" />
    
    <item
        android:id="@+id/action_sort"
        android:icon="@android:drawable/ic_menu_sort_by_size"
        android:title="Ordenar"
        app:showAsAction="ifRoom">
        
        <menu>
            <item
                android:id="@+id/sort_by_date"
                android:title="Por fecha (más recientes)" />
            
            <item
                android:id="@+id/sort_by_date_oldest"
                android:title="Por fecha (más antiguos)" />
            
            <item
                android:id="@+id/sort_by_title"
                android:title="Por título (A-Z)" />
            
            <item
                android:id="@+id/sort_by_title_desc"
                android:title="Por título (Z-A)" />
        </menu>
    </item>
</menu> 